services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: businessdb
      POSTGRES_USER: business
      POSTGRES_PASSWORD: business_dev_password
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"

volumes:
  postgres_data:
